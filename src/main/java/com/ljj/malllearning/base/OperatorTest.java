package com.ljj.malllearning.base;

import java.net.InetAddress;
import java.util.ArrayList;
import java.util.Arrays;
import java.util.Collections;
import java.util.HashMap;
import java.util.LinkedList;
import java.util.List;
import java.util.Map;
import java.util.concurrent.CopyOnWriteArrayList;
import java.util.stream.Collectors;

import com.alibaba.fastjson.JSONObject;

import com.ljj.malllearning.java8.A;
import lombok.SneakyThrows;

/**
 * @author ljj
 * @date 2021/10/18
 */
public class OperatorTest {

    @SneakyThrows
    public static void main(String[] args) {
        //if (true && false && !false || true && false) {
        //    System.out.println("true");
        //} else {
        //    System.out.println("false");
        //}

        //System.out.println(JSONObject.toJSONString(InetAddress.getLocalHost()));

        //Map<String, Map<String, String>> map=new HashMap<>();
        //Map<String, String> map1=new HashMap<>();
        //map1.put("1","1");
        //map.put("1",map1);
        //
        //Map<String, String> map2=new HashMap<>();
        //map2.put("2","2");
        //map.put("2",map2);
        //System.out.println(map.values().stream().map(value -> value.values()).flatMap(v -> v.stream()).collect(
        //    Collectors.toList()));
        //Map<String,String> map=new HashMap<>();
        //map.put("abc","abc");
        //Map<String,String> map2=new HashMap<>();
        //map2.put("abc","abc");
        //
        //System.out.println(map.hashCode());
        //System.out.println(map2.hashCode());
        //Collections.synchronizedList();


    }
}
